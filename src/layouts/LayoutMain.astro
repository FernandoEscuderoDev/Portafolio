---
import Nav from "../components/header/Nav.jsx";
import ImgAnimation from "../components/header/ImgAnimation.jsx";
import { motion } from "framer-motion";
import Footer from "../components/footer/Footer.jsx";
---

<!DOCTYPE html>
<html lang="es" class="overflow-x-hidden">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FernDev</title>
    <link rel="shortcut icon" href="favicon.png" type="image/x-icon" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body
    class="bg-gradient-to-b from-zinc-900 via-zinc-900 to-zinc-800 text-zinc-100 font-['Roboto'] relative min-h-screen overflow-x-hidden"
  >
    <header class="h-screen w-full">
      <Nav client:visible />
      <div
        class="container h-[80%] flex flex-col-reverse items-center justify-center gap-7 md:flex-row lg:justify-around"
      >
        <article
          class="w-96 md:h-80 flex flex-col justify-center gap-4 md:gap-10"
        >
          <motion.div
            className="overflow-hidden border-r-4 border-manz-500"
            initial={{ width: "0px", opacity: 1, border: "none" }}
            animate={{
              scale: 1,
              width: "98%",
              borderRight: "4px #E1E111 solid",
            }}
            transition={{ delay: 0.1, duration: 2, ease: "easeInOut" }}
            client:visible
          >
            <h1 class="font-bold text-5xl w-96 text-zinc-50">
              Fern - Front-End
            </h1>
          </motion.div>
          <motion.p
            client:visible
            className="font-medium text-lg"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.1, duration: 1.5, ease: "easeInOut" }}
          >
            Hola!, Bienvenido a mi pagina personal, me llamo <span
              class="text-manz-500">Fernando</span
            > tengo 19 años y soy de Argentina. Desarrollo paginas web con Frameworks
            como <span class="text-manz-500">Astro</span> y
            <span class="text-manz-500">React</span>.
          </motion.p>
        </article>
        <div class="sm:w-80 h-80 w-full">
          <ImgAnimation client:visible>
            <img
              class="rounded-xl w-64 md:w-80 mx-auto sombra"
              src="https://picsum.photos/320"
              alt=""
            />
          </ImgAnimation>
        </div>
      </div>
      <motion.div
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.1, duration: 1.5, ease: "easeInOut" }}
        className="w-full flex justify-center text-2xl text-zinc-50"
        client:visible
      >
        <i class="fa-solid fa-arrow-down animate-bounce cursor-pointer pb-8"
        ></i>
      </motion.div>
    </header>
    <slot />
    <Footer client:visible />
    <script
      src="https://kit.fontawesome.com/3cabd74866.js"
      crossorigin="anonymous"
    ></script>
  </body>
  <style is:global>
    .sombra {
      box-shadow: -10px 7px 0 2px rgb(39, 39, 42, 0.95),
        -20px 14px 0 2px rgb(29, 29, 32, 0.95),
        -30px 22px 0 2px rgba(24, 24, 27, 0.95);
      transition: all 500ms ease-out;
    }
    .sombra:hover {
      box-shadow: none;
    }
    .efectoTarjeta {
      background-image: url("https://picsum.photos/200/300?random=1");
      background: linear-gradient(90deg, #e1e11110 5%, #4545051f 100%);
      clip-path: polygon(0 0, 100% 0, 120% 15%, 0 50%);
    }
    .efectoTarjetaPrincipal {
      background: linear-gradient(90deg, #374151 5%, #111827 100%);
    }
  </style>
  <script>
    const iconoFlecha = document.querySelector(".fa-arrow-down");
    iconoFlecha?.addEventListener("click", () => {
      window.scrollTo({ top: 650, behavior: "smooth" });
    });
  </script>
</html>
