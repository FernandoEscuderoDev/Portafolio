---
import Nav from "../components/header/Nav.jsx";
import ImgAnimation from "../components/header/ImgAnimation.jsx";
import { motion } from "framer-motion";
const variants = {
  visible: {
    "::after": {
      width: "50px",
      backgroundColor: "red",
    },
  },
};
---

<!DOCTYPE html>
<html lang="es" class="overflow-x-hidden">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FernDev</title>
    <link rel="shortcut icon" href="favicon.png" type="image/x-icon" />
    <meta name="generator" content={Astro.generator} />
  </head>
  <body
    class="bg-gradient-to-b from-zinc-900 via-zinc-900 to-zinc-800 text-zinc-200 bg-zinc-900 font-sans relative min-h-screen overflow-x-hidden"
  >
    <header class="h-screen">
      <Nav client:load />
      <div
        class="container h-[80%] flex flex-col-reverse items-center justify-center gap-10 md:flex-row lg:justify-around"
      >
        <article
          class="w-96 md:h-80 flex flex-col justify-center gap-5 md:gap-16"
        >
          <motion.div
            className="overflow-hidden border-r-4 border-manz-500"
            initial={{ width: "0px", opacity: 1, border: "none" }}
            animate={{
              scale: 1,
              width: "100%",
              borderRight:"4px #E1E111 solid"
            }}
            transition={{ delay: 0.15, duration: 2, ease: "easeInOut" }}
            client:visible
          >
            <h1 class="font-bold text-5xl w-96 text-zinc-50">Fern - Front-End</h1>
          </motion.div>
          <motion.p
            client:load
            className="font-medium text-lg"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.15, duration: 1.5, ease: "easeInOut" }}
          >
            Hola!, Bienvenido a mi pagina personal, me llamo <span
              class="text-manz-500">Fernando</span
            > tengo 19 años y soy de Argentina. Desarrollo paginas web con Frameworks
            como <span class="text-manz-500">Astro</span> y
            <span class="text-manz-500">React</span>.
          </motion.p>
        </article>
        <div class="w-80 h-80">
          <ImgAnimation client:visible>
            <img
              class="rounded-xl w-64 md:w-80"
              src="https://picsum.photos/320"
              alt=""
            />
          </ImgAnimation>
        </div>
      </div>
      <div class="w-full flex justify-center text-2xl text-zinc-50">
        <a href="">
          <i class="fa-solid fa-arrow-down animate-bounce"></i>
        </a>
      </div>
    </header>
    <slot />
    <script
      src="https://kit.fontawesome.com/3cabd74866.js"
      crossorigin="anonymous"
    ></script>
  </body>

  <style>
    img {
      box-shadow:
        -10px 7px 0 2px rgb(39, 39, 42, 0.95),
        -20px 14px 0 2px rgb(29, 29, 32, 0.95),
        -30px 22px 0 2px rgba(24, 24, 27, 0.95);
      transition: all 500ms ease-out;
    }
    img:hover {
      box-shadow: none;
    }
  </style>
</html>
